import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { ReactTimerBar } from './ReactTimerBar';

ReactDOM.render(<ReactTimerBar name="Anthony Thuo"/>, document.getElementById('output'));
